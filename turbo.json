{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["lint", "check-types", "^build"],
      "outputs": ["dist/**"]
    },
    "check-types": {
      "dependsOn": ["parallel-with-deps"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "parallel-with-deps": {
      "dependsOn": ["^parallel-with-deps"]
    },
    "start": {
      "persistent": true
    }
  }
}
