{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["lint", "check-types", "^build"],
      "outputs": ["dist/**"]
    },
    "check-types": {
      "dependsOn": ["parallel-with-deps"]
    },
    "dev": {
      "cache": false,
      "dependsOn": ["check-types", "lint"],
      "persistent": true
    },
    "lint": {},
    "parallel-with-deps": {
      "dependsOn": ["^parallel-with-deps"]
    },
    "start": {
      "persistent": true
    }
  }
}
