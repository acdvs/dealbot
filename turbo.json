{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["lint", "^build"],
      "outputs": ["dist/**"]
    },
    "api#build": {
      "dependsOn": ["update-types"]
    },
    "web#build": {
      "dependsOn": ["lint"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "dev": {},
    "lint": {},
    "update-types": {}
  }
}
