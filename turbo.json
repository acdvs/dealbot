{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["check-types", "lint", "^build"],
      "outputs": ["dist/**"]
    },
    "check-types": {},
    "dev": {
      "cache": false,
      "dependsOn": ["check-types"],
      "persistent": true
    },
    "lint": {},
    "start": {},
    "@dealbot/api#update-types": {
      "outputs": ["src/types.ts"]
    }
  }
}
